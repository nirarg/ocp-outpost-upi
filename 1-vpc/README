Launch the template:

aws cloudformation create-stack --stack-name nargaman-cluster-vpc --template-body file://template.yaml --parameters file://parameters.json


Confirm that the template components exist:

aws cloudformation describe-stacks --stack-name nargaman-cluster-vpc


Output:
* VpcId
* PublicSubnetIds
* PrivateSubnetIds
